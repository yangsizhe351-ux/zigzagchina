# ZigZag China 发布证据清单

本文件是发布时的检查矩阵，不是任务状态台账。当前是否可发布、候选 SHA、审核结论和风险只以 `TASKS.yaml` 与 `HANDOFF.md` 为准。

## 历史基线证据

以下证据来自 2026-08-30 对历史候选 `bca4e4c` 的检查，不自动覆盖当前 `main`：

- [x] 三语内容检查和 Netlify 静态构建通过
- [x] `/`、`/about`、`/business-credentials` 与未知路径 404 通过
- [x] 桌面端、390px 和 320px 视口检查通过
- [x] 移动菜单、城市筛选、体验弹层和邮件预约链路通过
- [x] 浏览器控制台无错误

## 当前候选封板

- [ ] `TASKS.yaml` 已记录唯一候选、测试、风险和预声明 `gate_ref`
- [ ] 工作区干净，`HEAD` 与待审精确 SHA 一致
- [ ] 独立验收对精确 SHA 给出 `PASS`
- [ ] 台账收口提交再次通过独立验收
- [ ] 本地 annotated `gate_ref` 精确指向最终 `HEAD`
- [ ] 不存在 P0/P1、未确认公开事实或未接受风险

## 正式推广前的业务确认

- [ ] 用户确认目标是预览验证还是正式推广发布
- [ ] 资质信息、中法文翻译和法定代表人姓名公开授权已确认
- [ ] 公开联系邮箱和邮件主题已确认
- [ ] 隐私、条款、取消/改期、服务范围和回复承诺已确认
- [ ] 是否启用 Supabase 已决定；如启用，seed 中时长等数据已有真实来源
- [ ] 正式域名、canonical、sitemap、品牌邮箱和社交分享元数据已确定

## 用户授权后的部署检查

- [ ] 用户明确授权指定候选 SHA 上线
- [ ] `HEAD == gate_ref^{commit}`，且没有 tag 之后的 commit、merge、amend 或 rebase
- [ ] 只 push 已授权的 `main`；不上传本地 `dist-netlify/`
- [ ] Netlify 构建成功且部署 SHA 与授权候选一致
- [ ] 线上 `/`、`/about`、`/business-credentials` 和真实 404 通过
- [ ] 线上三语、移动端、关键交互、资源和控制台通过
- [ ] 线上失败时停止后续发布，使用可追溯回滚；不强推或改写历史

## 明确排除的范围

搜索、路线详情、旅程保存/分享、实用指南和候补名单不是当前发布待办，除非用户明确改变产品范围。
